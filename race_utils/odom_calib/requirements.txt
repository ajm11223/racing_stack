# Offline analysis only — no ROS needed (messages are decoded straight from the .mcap schema).
numpy>=1.24
scipy>=1.10
matplotlib>=3.6
mcap>=1.1
mcap-ros2-support>=0.5
# optional, for APE/RPE on the exported TUM trajectories:
# evo>=1.30
